#include <iostream>
#include <map>
#include <queue>
using namespace std;

int main(){
    cin.tie(0);
    cin.sync_with_stdio(0);

    int n;
    cin >> n;

    while(n--){
        
    }

    return 0;
}